import { SignupClient } from "@/components/auth-forms";

export const metadata = {
  title: "Get started",
  robots: { index: false, follow: false },
};

export default function SignupPage() {
  return <SignupClient />;
}
